# Local/VPS Socket.IO uses /socket.io by default.
# Vercel Socket.IO is exposed through the dedicated function route below.
VITE_SOCKET_PATH=/socket.io

# On Vercel set:
# VITE_SOCKET_PATH=/api/socket-io/socket.io
