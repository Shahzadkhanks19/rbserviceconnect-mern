import {Ban,IndianRupee,ReceiptText,Rocket,UsersRound} from 'lucide-react';import {useEffect,useState} from 'react';import ActionModal from '../../components/ActionModal.jsx';import {apiRequest} from '../../lib/api.js';
const productNames={'candidate-profile-boost':'Profile Boost','job-featured':'Featured Job','job-search-boost':'Search Boost','job-urgent':'Urgent Hiring'};const money=(value)=>`₹${Number(value||0).toLocaleString('en-IN')}`;
export default function AdminPromotionsPage(){const [data,setData]=useState(null);const [filters,setFilters]=useState({status:'',audience:''});const [message,setMessage]=useState('');const [target,setTarget]=useState(null);const load=async()=>{const query=new URLSearchParams(Object.entries(filters).filter(([,value])=>value));setData(await apiRequest(`/admin/promotions?${query}`));};useEffect(()=>{let cancelled=false;const query=new URLSearchParams(Object.entries(filters).filter(([,value])=>value));apiRequest(`/admin/promotions?${query}`).then((response)=>!cancelled&&setData(response)).catch((error)=>!cancelled&&setMessage(error.message));return()=>{cancelled=true;};},[filters]);const cancel=async()=>{if(!target)return;try{const response=await apiRequest(`/admin/promotions/${target._id}/cancel`,{method:'PATCH'});setMessage(response.message);setTarget(null);await load();}catch(error){setMessage(error.message);}};if(!data)return <div className="rounded-2xl border border-slate-200 bg-white p-8 text-sm text-slate-500">{message||'Loading monetization…'}</div>;const active=data.summary.statuses.find((item)=>item._id==='active')?.count||0;const pending=data.summary.statuses.find((item)=>item._id==='pending')?.count||0;return <section className="space-y-6"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-amber-700">Monetization</p><h2 className="mt-2 text-3xl font-semibold tracking-tight text-slate-950">Promotion payments</h2><p className="mt-2 text-sm text-slate-500">Track verified Razorpay promotion revenue, transactions, active placements and expiry periods.</p></div>{message&&<div className="rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm text-slate-600">{message}</div>}<div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat icon={IndianRupee} label="Verified revenue" value={money(data.summary.revenue)}/><Stat icon={ReceiptText} label="Paid transactions" value={data.summary.paidTransactions}/><Stat icon={Rocket} label="Active promotions" value={active}/><Stat icon={UsersRound} label="Pending orders" value={pending}/></div><div className="flex flex-wrap gap-3 rounded-2xl border border-slate-200 bg-white p-4"><Filter label="Status" value={filters.status} onChange={(value)=>setFilters((x)=>({...x,status:value}))} options={['pending','active','expired','cancelled']}/><Filter label="Audience" value={filters.audience} onChange={(value)=>setFilters((x)=>({...x,audience:value}))} options={['candidate','recruiter']}/></div><div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-4">Customer</th><th className="px-5 py-4">Promotion</th><th className="px-5 py-4">Payment</th><th className="px-5 py-4">Period</th><th className="px-5 py-4">Status</th><th className="px-5 py-4 text-right">Action</th></tr></thead><tbody className="divide-y divide-slate-100">{data.promotions.map((item)=><tr key={item._id} className="align-top"><td className="px-5 py-4"><strong className="text-slate-900">{item.owner?`${item.owner.firstName} ${item.owner.lastName}`:'Unknown user'}</strong><p className="mt-1 text-xs text-slate-500">{item.owner?.email||'—'} · {item.audience}</p></td><td className="px-5 py-4"><strong>{productNames[item.product]||item.product}</strong><p className="mt-1 text-xs text-slate-500">{item.durationDays} days · {money(item.amount)}</p></td><td className="px-5 py-4"><span className="text-xs font-medium text-slate-700">{item.payment?.paymentId||'Not paid'}</span><p className="mt-1 text-xs text-slate-400">{item.payment?.paidAt?new Date(item.payment.paidAt).toLocaleString():'Awaiting verified payment'}</p></td><td className="px-5 py-4 text-xs text-slate-500">{item.startsAt?new Date(item.startsAt).toLocaleDateString():'—'} → {item.endsAt?new Date(item.endsAt).toLocaleDateString():'—'}</td><td className="px-5 py-4"><Status value={item.status}/></td><td className="px-5 py-4 text-right">{['pending','active'].includes(item.status)&&<button type="button" onClick={()=>setTarget(item)} className="inline-flex items-center gap-1 rounded-lg border border-rose-200 px-3 py-2 text-xs font-semibold text-rose-700 hover:bg-rose-50"><Ban size={14}/>Cancel</button>}</td></tr>)}</tbody></table></div>{!data.promotions.length&&<div className="p-10 text-center text-sm text-slate-400">No promotion transactions match these filters.</div>}</div><div className="rounded-2xl border border-slate-200 bg-white p-5"><h3 className="font-semibold text-slate-900">Revenue by promotion</h3><div className="mt-4 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{data.summary.products.map((item)=><div key={item._id} className="rounded-xl bg-slate-50 p-4"><p className="text-xs text-slate-500">{productNames[item._id]||item._id}</p><strong className="mt-1 block text-xl text-slate-900">{money(item.revenue)}</strong><span className="text-xs text-slate-400">{item.transactions} paid transaction{item.transactions===1?'':'s'}</span></div>)}</div></div><ActionModal open={Boolean(target)} title="Cancel promotion?" description="This immediately stops the platform promotion. It does not refund the Razorpay payment; refunds must be handled separately and deliberately." confirmLabel="Cancel promotion" tone="danger" onConfirm={cancel} onClose={()=>setTarget(null)}/></section>}
function Stat({icon:Icon,label,value}){return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">{label}</p><Icon size={18} className="text-amber-600"/></div><strong className="mt-3 block text-2xl text-slate-950">{value}</strong></div>}function Filter({label,value,onChange,options}){return <label className="text-xs font-semibold text-slate-600">{label}<select value={value} onChange={(event)=>onChange(event.target.value)} className="ml-2 rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm font-normal outline-none focus:border-amber-400"><option value="">All</option>{options.map((option)=><option key={option} value={option}>{option}</option>)}</select></label>}function Status({value}){return <span className={`rounded-full px-2.5 py-1 text-xs font-semibold capitalize ${value==='active'?'bg-emerald-100 text-emerald-700':value==='pending'?'bg-amber-100 text-amber-700':value==='cancelled'?'bg-rose-100 text-rose-700':'bg-slate-100 text-slate-600'}`}>{value}</span>}
