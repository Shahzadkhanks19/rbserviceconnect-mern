import { Save, Settings } from 'lucide-react';
import { useEffect, useState } from 'react';
import { apiRequest } from '../../lib/api.js';

export default function AdminSettingsPage(){const [form,setForm]=useState(null);const [message,setMessage]=useState('');const [saving,setSaving]=useState(false);useEffect(()=>{let cancelled=false;apiRequest('/admin/settings').then(r=>!cancelled&&setForm(r.settings)).catch(e=>!cancelled&&setMessage(e.message));return()=>{cancelled=true;};},[]);if(!form)return <div className="rounded-2xl border border-slate-200 bg-white p-10 text-center text-sm text-slate-500">{message||'Loading settings…'}</div>;const toggle=(key)=>setForm(f=>({...f,[key]:!f[key]}));const save=async()=>{setSaving(true);setMessage('');try{const r=await apiRequest('/admin/settings',{method:'PUT',body:JSON.stringify(form)});setForm(r.settings);setMessage(r.message);}catch(e){setMessage(e.message);}finally{setSaving(false);}};return <section className="space-y-6"><div className="rounded-[2rem] bg-[#879E83] p-6 sm:p-8"><div className="flex items-start gap-4"><span className="grid size-12 place-items-center rounded-2xl bg-white/20"><Settings size={22}/></span><div><p className="text-xs font-bold uppercase tracking-[.18em] text-white/80">Platform settings</p><h2 className="mt-2 text-3xl font-semibold tracking-[-.04em] text-slate-950 sm:text-4xl">Control platform-wide access rules.</h2></div></div></div>{message&&<div className="rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm text-slate-600">{message}</div>}<div className="rounded-[1.7rem] border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="space-y-3">{[['candidateRegistrationOpen','Candidate registration','Allow new candidate accounts to register.'],['recruiterRegistrationOpen','Recruiter registration','Allow new recruiter accounts to register.'],['requireRecruiterApproval','Recruiter approval','Require admin approval before recruiter access is enabled.'],['maintenanceMode','Maintenance mode','Prepare a platform-wide maintenance state for deployment operations.']].map(([key,title,copy])=><button key={key} type="button" onClick={()=>toggle(key)} className="flex w-full items-center justify-between gap-5 rounded-2xl border border-slate-200 p-4 text-left"><span><strong className="block text-sm text-slate-900">{title}</strong><span className="mt-1 block text-xs leading-5 text-slate-500">{copy}</span></span><span className={`relative h-7 w-12 shrink-0 rounded-full transition ${form[key]?'bg-[#E3A341]':'bg-slate-300'}`}><span className={`absolute top-1 size-5 rounded-full bg-white transition ${form[key]?'left-6':'left-1'}`}/></span></button>)}</div><label className="mt-6 block text-sm font-semibold text-slate-700"><span className="mb-2 block">Support notice</span><textarea rows="4" maxLength="300" value={form.supportMessage||''} onChange={e=>setForm(f=>({...f,supportMessage:e.target.value}))} className="w-full resize-none rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-amber-400" placeholder="Optional message for operational/support communication."/></label><button type="button" disabled={saving} onClick={save} className="mt-6 inline-flex items-center gap-2 rounded-xl bg-slate-950 px-5 py-3 text-sm font-semibold text-white disabled:opacity-60"><Save size={16}/>{saving?'Saving…':'Save platform settings'}</button></div></section>}
