import { Activity,BriefcaseBusiness,Building2,FileCheck2,Mail,ShieldCheck,UserRound,UsersRound } from 'lucide-react';
import { useEffect,useState } from 'react';
import { Link } from 'react-router-dom';
import { apiRequest } from '../../lib/api.js';

export default function AdminOverviewPage(){const [data,setData]=useState(null);const [error,setError]=useState('');useEffect(()=>{let cancelled=false;apiRequest('/admin/dashboard').then((r)=>!cancelled&&setData(r)).catch((e)=>!cancelled&&setError(e.message));return()=>{cancelled=true;};},[]);if(!data)return <div className="rounded-2xl border border-slate-200 bg-white p-10 text-center text-sm text-slate-500">{error||'Loading platform overview…'}</div>;const c=data.counts;const cards=[[UsersRound,'Users',c.users],[UserRound,'Candidates',c.candidates],[ShieldCheck,'Recruiters',c.recruiters],[Building2,'Companies',c.companies],[BriefcaseBusiness,'Published jobs',c.publishedJobs],[FileCheck2,'Applications',c.applications],[FileCheck2,'Hires',c.hires],[Mail,'New enquiries',c.newContacts]];return <section className="space-y-7"><div className="rounded-[2rem] bg-slate-950 p-6 text-white sm:p-8"><div className="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#F3E8A2]">Platform control center</p><h2 className="mt-3 text-3xl font-semibold tracking-[-.04em] sm:text-4xl">See what is happening across RB Service Connect.</h2><p className="mt-3 max-w-2xl text-sm leading-7 text-slate-400">Operational totals, moderation queues, hiring activity, enquiries and administrative actions from the live platform data store.</p></div><Link to="/admin/activity" className="inline-flex items-center gap-2 self-start rounded-xl border border-white/15 px-4 py-2.5 text-xs font-semibold text-white transition hover:-translate-y-0.5 hover:border-white/30 hover:bg-white/10 lg:self-auto"><Activity size={15}/> View audit activity</Link></div></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([Icon,label,value])=><article key={label} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition duration-300 hover:-translate-y-1 hover:border-indigo-200 hover:shadow-lg"><span className="grid size-10 place-items-center rounded-xl bg-[#F3E8A2]/65 text-indigo-700"><Icon size={18}/></span><strong className="mt-5 block text-2xl text-slate-950">{value}</strong><span className="mt-1 block text-sm text-slate-500">{label}</span></article>)}</div><div className="grid gap-6 xl:grid-cols-2"><Panel title="Needs attention" description="Queues and operational areas that may require review."><div className="grid gap-3 sm:grid-cols-2"><Quick to="/admin/recruiters" label="Pending recruiters" value={c.pendingRecruiters}/><Quick to="/admin/contacts" label="New enquiries" value={c.newContacts}/><Quick to="/admin/jobs" label="Jobs in review" value={c.reviewingJobs}/><Quick to="/admin/applications" label="Total applications" value={c.applications}/></div></Panel><Panel title="Recent registrations" description="Latest candidate, recruiter and admin account creation activity."><div className="divide-y divide-slate-100">{data.recentUsers.length?data.recentUsers.map((u)=><div key={u._id} className="py-3 transition hover:bg-slate-50/70"><div className="flex items-center justify-between gap-3 px-1"><div className="min-w-0"><strong className="block truncate text-sm text-slate-900">{u.firstName} {u.lastName}</strong><span className="block truncate text-xs text-slate-500">{u.email}</span></div><span className="rounded-full bg-slate-100 px-2.5 py-1 text-[11px] font-semibold capitalize text-slate-600">{u.role}</span></div></div>):<p className="py-6 text-sm text-slate-500">No recent registrations.</p>}</div></Panel></div><Panel title="Recent admin activity" description="Latest audited operational changes made from administrator workflows."><div className="space-y-3">{data.recentActivity.length?data.recentActivity.map((a)=><div key={a._id} className="rounded-2xl bg-slate-50 p-4 transition hover:-translate-y-0.5 hover:bg-indigo-50/50"><p className="text-sm font-medium text-slate-800">{a.description}</p><p className="mt-1 text-xs text-slate-400">{a.admin?.firstName} {a.admin?.lastName} · {new Date(a.createdAt).toLocaleString()}</p></div>):<p className="text-sm text-slate-500">No admin actions recorded yet.</p>}</div></Panel></section>}
function Panel({title,description,children}){return <section className="rounded-[1.7rem] border border-slate-200 bg-white p-5 shadow-sm transition duration-300 hover:border-slate-300 hover:shadow-md sm:p-6"><h3 className="text-lg font-semibold text-slate-950">{title}</h3>{description&&<p className="mt-1 text-sm leading-6 text-slate-500">{description}</p>}<div className="mt-4">{children}</div></section>}
function Quick({to,label,value}){return <Link to={to} className="group rounded-2xl border border-slate-200 p-4 transition duration-300 hover:-translate-y-0.5 hover:border-amber-300 hover:bg-amber-50/30 hover:shadow-sm"><strong className="block text-2xl text-slate-950">{value}</strong><span className="mt-1 block text-sm text-slate-500 transition group-hover:text-slate-700">{label}</span></Link>}
