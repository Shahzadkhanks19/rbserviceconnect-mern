import { CheckCircle2, Search, Trash2 } from 'lucide-react';
import { useEffect, useMemo, useState } from 'react';
import ActionModal from '../../components/ActionModal.jsx';
import { apiRequest } from '../../lib/api.js';

const configs={
  users:{title:'Users',copy:'Manage candidate, recruiter, and administrator account access.',endpoint:'/admin/users',key:'users',filters:['all','candidate','recruiter','admin']},
  companies:{title:'Companies',copy:'Review employer profiles and verification states.',endpoint:'/admin/companies',key:'companies',filters:['all','verified','pending','rejected']},
  jobs:{title:'Jobs',copy:'Moderate jobs across every recruiter and hiring lifecycle stage.',endpoint:'/admin/jobs',key:'jobs',filters:['all','draft','published','reviewing','paused','closed']},
  applications:{title:'Applications',copy:'Inspect candidate applications and hiring pipeline outcomes.',endpoint:'/admin/applications',key:'applications',filters:['all','applied','reviewing','shortlisted','interview','offered','hired','rejected','withdrawn']},
  contacts:{title:'Contact enquiries',copy:'Review and resolve enquiries submitted from the public website.',endpoint:'/admin/contacts',key:'contacts',filters:['all','new','reviewed','resolved']},
  activity:{title:'Admin activity log',copy:'Audit sensitive platform-management actions.',endpoint:'/admin/activity',key:'activity',filters:['all']},
};
export default function AdminCollectionPage({resource}){const cfg=configs[resource];const [items,setItems]=useState(null);const [filter,setFilter]=useState('all');const [query,setQuery]=useState('');const [message,setMessage]=useState('');const [modal,setModal]=useState(null);const load=async()=>{try{const params=new URLSearchParams();if(filter!=='all'){if(resource==='users')params.set('role',filter);else params.set('status',filter);}if(query.trim()&&['users','companies','jobs','contacts'].includes(resource))params.set('q',query.trim());params.set('limit','100');const r=await apiRequest(`${cfg.endpoint}?${params}`);setItems(r[cfg.key]||[]);}catch(e){setMessage(e.message);}};useEffect(()=>{let cancelled=false;const params=new URLSearchParams();if(filter!=='all'){if(resource==='users')params.set('role',filter);else params.set('status',filter);}if(query.trim()&&['users','companies','jobs','contacts'].includes(resource))params.set('q',query.trim());params.set('limit','100');apiRequest(`${cfg.endpoint}?${params}`).then(r=>!cancelled&&setItems(r[cfg.key]||[])).catch(e=>!cancelled&&setMessage(e.message));return()=>{cancelled=true;};},[cfg.endpoint,cfg.key,filter,query,resource]);const rows=useMemo(()=>items||[],[items]);const updateStatus=async(item,status)=>{try{let path;if(resource==='users')path=`/admin/users/${item._id}/status`;else if(resource==='companies')path=`/admin/companies/${item._id}/status`;else if(resource==='jobs')path=`/admin/jobs/${item._id}/status`;else if(resource==='contacts')path=`/admin/contacts/${item._id}/status`;else return;const r=await apiRequest(path,{method:'PATCH',body:JSON.stringify({status})});setMessage(r.message);await load();}catch(e){setMessage(e.message);}};const removeContact=async()=>{if(!modal)return;try{const r=await apiRequest(`/admin/contacts/${modal._id}`,{method:'DELETE'});setMessage(r.message);setModal(null);await load();}catch(e){setMessage(e.message);}};return <section className="space-y-6"><div className="rounded-[2rem] bg-[#879E83] p-6 sm:p-8"><p className="text-xs font-bold uppercase tracking-[.18em] text-white/80">Administration</p><h2 className="mt-3 text-3xl font-semibold tracking-[-.04em] text-slate-950 sm:text-4xl">{cfg.title}</h2><p className="mt-3 max-w-2xl text-sm leading-7 text-slate-900/70">{cfg.copy}</p></div>{message&&<div className="rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm text-slate-600">{message}</div>}<div className="flex flex-col gap-3 rounded-2xl border border-slate-200 bg-white p-4 sm:flex-row sm:items-center sm:justify-between">{['users','companies','jobs','contacts'].includes(resource)?<label className="flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-2.5 sm:min-w-72"><Search size={16} className="text-slate-400"/><input value={query} onChange={e=>setQuery(e.target.value)} className="min-w-0 flex-1 text-sm outline-none" placeholder={`Search ${cfg.title.toLowerCase()}…`}/></label>:<div/>}<div className="flex flex-wrap gap-2">{cfg.filters.map(x=><button key={x} type="button" onClick={()=>setFilter(x)} className={`rounded-xl px-3 py-2 text-xs font-semibold capitalize ${filter===x?'bg-slate-950 text-white':'bg-slate-100 text-slate-600'}`}>{x}</button>)}</div></div><div className="overflow-hidden rounded-[1.7rem] border border-slate-200 bg-white shadow-sm">{!items?<div className="p-10 text-center text-sm text-slate-500">Loading…</div>:!rows.length?<div className="p-10 text-center text-sm text-slate-500">No records in this view.</div>:<div className="divide-y divide-slate-100">{rows.map(item=><Row key={item._id} resource={resource} item={item} onStatus={updateStatus} onDelete={()=>setModal(item)}/>)}</div>}</div><ActionModal open={Boolean(modal)} title="Delete this enquiry?" description="This contact enquiry will be permanently removed from the platform." confirmLabel="Delete enquiry" tone="danger" onClose={()=>setModal(null)} onConfirm={removeContact}/></section>}
function Row({resource,item,onStatus,onDelete}){if(resource==='users')return <Base title={`${item.firstName} ${item.lastName}`} subtitle={`${item.email} · ${item.role}`} meta={`Joined ${new Date(item.createdAt).toLocaleDateString()}`} badge={item.status}><Actions>{item.role!=='admin'&&item.status!=='active'&&<Btn onClick={()=>onStatus(item,'active')}>Activate</Btn>}{item.role!=='admin'&&item.status!=='suspended'&&<Btn danger onClick={()=>onStatus(item,'suspended')}>Suspend</Btn>}</Actions></Base>;if(resource==='companies')return <Base title={item.name} subtitle={`${item.industry||'Industry not set'} · ${item.location||'Location not set'}`} meta={`${item.jobCount||0} jobs · ${item.owner?.email||'No owner'}`} badge={item.verificationStatus}><Actions>{item.verificationStatus!=='verified'&&<Btn onClick={()=>onStatus(item,'verified')}>Verify</Btn>}{item.verificationStatus!=='rejected'&&<Btn danger onClick={()=>onStatus(item,'rejected')}>Reject</Btn>}</Actions></Base>;if(resource==='jobs')return <Base title={item.title} subtitle={`${item.company?.name||'Company'} · ${item.createdBy?.email||'Recruiter'}`} meta={`${item.applicants||0} applicants · ${item.workMode} · ${item.employmentType}`} badge={item.status}><Actions>{item.status!=='closed'&&<Btn danger onClick={()=>onStatus(item,'closed')}>Close</Btn>}{item.status==='paused'&&<Btn onClick={()=>onStatus(item,'published')}>Republish</Btn>}</Actions></Base>;if(resource==='applications')return <Base title={item.job?.title||item.jobSnapshot?.title||'Application'} subtitle={`${item.candidate?.firstName||''} ${item.candidate?.lastName||''} · ${item.candidate?.email||''}`} meta={`${item.job?.company?.name||item.jobSnapshot?.company||'Employer'} · Applied ${new Date(item.createdAt).toLocaleDateString()}`} badge={item.status}/>;if(resource==='contacts')return <Base title={item.name} subtitle={`${item.email} · ${item.topic}`} meta={item.message} badge={item.status}><Actions>{item.status!=='reviewed'&&<Btn onClick={()=>onStatus(item,'reviewed')}>Reviewed</Btn>}{item.status!=='resolved'&&<Btn onClick={()=>onStatus(item,'resolved')}>Resolve</Btn>}<button type="button" onClick={onDelete} className="grid size-9 place-items-center rounded-xl border border-red-200 text-red-600"><Trash2 size={15}/></button></Actions></Base>;return <Base title={item.description} subtitle={`${item.admin?.firstName||'Admin'} ${item.admin?.lastName||''} · ${item.action}`} meta={new Date(item.createdAt).toLocaleString()}/>}
function Base({title,subtitle,meta,badge,children}){return <article className="grid gap-4 p-5 lg:grid-cols-[1fr_auto] lg:items-center"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h3 className="font-semibold text-slate-950">{title}</h3>{badge&&<span className="rounded-full bg-slate-100 px-2.5 py-1 text-[11px] font-bold capitalize text-slate-600">{badge}</span>}</div><p className="mt-1 text-sm text-slate-500">{subtitle}</p>{meta&&<p className="mt-2 line-clamp-2 text-xs leading-5 text-slate-400">{meta}</p>}</div>{children}</article>}
function Actions({children}){return <div className="flex flex-wrap gap-2 lg:justify-end">{children}</div>}
function Btn({children,onClick,danger=false}){return <button type="button" onClick={onClick} className={`rounded-xl border px-3.5 py-2 text-xs font-semibold ${danger?'border-red-200 text-red-700':'border-slate-200 text-slate-700'}`}>{children}</button>}
