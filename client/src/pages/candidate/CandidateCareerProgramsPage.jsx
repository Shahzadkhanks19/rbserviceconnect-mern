import { Check,ChevronRight,CircleCheckBig,ShieldCheck,Sparkles,Target,UsersRound } from 'lucide-react';
import { useEffect,useMemo,useState } from 'react';
import { apiRequest } from '../../lib/api.js';

const money=(n)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(n||0);
export default function CandidateCareerProgramsPage(){const [data,setData]=useState(null);const [selected,setSelected]=useState('career-pro');const [payment,setPayment]=useState('full');const [accepted,setAccepted]=useState(false);const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);const load=()=>apiRequest('/candidate/career-programs').then(setData).catch((e)=>setMessage(e.message));useEffect(()=>{load();},[]);const program=useMemo(()=>data?.programs?.find((p)=>p.id===selected),[data,selected]);const plan=program?.paymentPlans?.find((p)=>p.id===payment)||program?.paymentPlans?.[0];const enroll=async()=>{setBusy(true);setMessage('');try{const r=await apiRequest('/candidate/career-programs/enroll',{method:'POST',body:JSON.stringify({program:selected,paymentPreference:payment,acceptTerms:accepted})});setMessage(r.message);await load();}catch(e){setMessage(e.message);}finally{setBusy(false);}};const withdraw=async()=>{setBusy(true);try{const r=await apiRequest('/candidate/career-programs/withdraw',{method:'PATCH'});setMessage(r.message);await load();}catch(e){setMessage(e.message);}finally{setBusy(false);}};if(!data)return <div className="rounded-3xl border border-slate-200 bg-white p-10 text-center text-sm text-slate-500">{message||'Loading Career Programmes…'}</div>;if(data.enrollment)return <Active enrollment={data.enrollment} programs={data.programs} onWithdraw={withdraw} busy={busy} message={message}/>;return <section className="space-y-6"><div className="overflow-hidden rounded-[2rem] bg-slate-950 p-6 text-white sm:p-8"><div className="grid gap-7 lg:grid-cols-[1fr_auto] lg:items-end"><div><span className="inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1.5 text-xs font-bold"><Sparkles size={14}/> RB Career Programmes</span><h1 className="mt-5 max-w-3xl text-3xl font-semibold tracking-[-.04em] sm:text-5xl">Career support with ₹0 upfront.</h1><p className="mt-4 max-w-2xl text-sm leading-7 text-slate-300 sm:text-base">Job search and normal applications stay free. Career Programmes are optional, hands-on placement services for candidates who want additional support.</p></div><div className="rounded-2xl bg-white/10 p-4 text-sm"><strong className="block text-lg">No placement, no success fee.</strong><span className="mt-1 block text-slate-300">A fee becomes payable only for an eligible programme-assisted placement after first salary is received.</span></div></div></div>{message&&<div className="rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm text-slate-600">{message}</div>}<div className="grid gap-4 xl:grid-cols-3">{data.programs.map((p)=><button key={p.id} type="button" onClick={()=>{setSelected(p.id);setPayment('full');}} className={`rounded-[1.6rem] border p-5 text-left transition ${selected===p.id?'border-indigo-400 bg-indigo-50/50 ring-2 ring-indigo-100':'border-slate-200 bg-white hover:border-slate-300'}`}><div className="flex items-center justify-between gap-3"><h2 className="text-xl font-semibold text-slate-950">{p.name}</h2>{p.id==='career-pro'&&<span className="rounded-full bg-indigo-500 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-white">Recommended</span>}</div><p className="mt-3 text-sm leading-6 text-slate-600">{p.description}</p><p className="mt-5 text-2xl font-bold text-slate-950">{money(p.successFeeAmount)} <span className="text-xs font-medium text-slate-500">after eligible placement</span></p><div className="mt-5 space-y-2">{p.services.map((s)=><span key={s} className="flex items-start gap-2 text-xs leading-5 text-slate-600"><Check size={14} className="mt-0.5 shrink-0 text-emerald-600"/>{s}</span>)}</div></button>)}</div><div className="grid gap-6 lg:grid-cols-[1fr_360px]"><div className="rounded-[1.6rem] border border-slate-200 bg-white p-5 sm:p-6"><h2 className="text-xl font-semibold text-slate-950">Choose how you'll pay after placement</h2><p className="mt-2 text-sm text-slate-500">Nothing is collected now. This preference is recorded with your enrollment.</p><div className="mt-5 grid gap-3 sm:grid-cols-3">{program?.paymentPlans.map((p)=><button key={p.id} type="button" onClick={()=>setPayment(p.id)} className={`rounded-2xl border p-4 text-left ${payment===p.id?'border-indigo-400 bg-indigo-50':'border-slate-200'}`}><strong className="block text-sm text-slate-950">{p.label}</strong><span className="mt-2 block text-lg font-bold text-slate-950">{p.installments===1?money(p.total):`${money(p.perInstallment)} × ${p.installments}`}</span><span className="mt-1 block text-xs text-slate-500">Total {money(p.total)}</span></button>)}</div><div className="mt-6 rounded-2xl bg-slate-50 p-4"><label className="flex cursor-pointer items-start gap-3"><input type="checkbox" checked={accepted} onChange={(e)=>setAccepted(e.target.checked)} className="mt-1 size-4 accent-indigo-600"/><span className="text-sm leading-6 text-slate-600">I understand this is an optional placement-assistance programme, normal job applications remain free, and I accept Career Programme Terms version <strong>{data.termsVersion}</strong>. I understand the agreed success fee applies only to an eligible programme-assisted placement after I receive my first salary.</span></label></div></div><aside className="rounded-[1.6rem] border border-slate-200 bg-white p-5"><p className="text-xs font-bold uppercase tracking-[.16em] text-indigo-600">Enrollment summary</p><h3 className="mt-3 text-xl font-semibold text-slate-950">{program?.name}</h3><div className="mt-5 space-y-3 text-sm"><Row label="Upfront today" value="₹0"/><Row label="Payment option" value={plan?.label}/><Row label="Success-fee total" value={money(plan?.total)}/></div><button type="button" disabled={!accepted||busy} onClick={enroll} className="mt-6 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-indigo-500 px-4 py-3 text-sm font-semibold text-white disabled:cursor-not-allowed disabled:opacity-40">{busy?'Enrolling…':'Enroll in programme'}<ChevronRight size={16}/></button><p className="mt-3 text-center text-[11px] leading-5 text-slate-400">Enrollment itself does not create a payment obligation.</p></aside></div><Trust/></section>}
function Active({enrollment,programs,onWithdraw,busy,message}){const p=programs.find((x)=>x.id===enrollment.program);const done=(enrollment.serviceProgress||[]).filter((x)=>x.completed).length;return <section className="space-y-6"><div className="rounded-[2rem] bg-[#879E83] p-6 sm:p-8"><p className="text-xs font-bold uppercase tracking-[.18em] text-white/80">Active career programme</p><h1 className="mt-3 text-3xl font-semibold tracking-[-.04em] text-slate-950 sm:text-4xl">{p?.name||enrollment.program}</h1><p className="mt-3 max-w-2xl text-sm leading-7 text-slate-900/70">Your placement-support workspace. Your agreed success fee is fixed from enrollment and is not based on your salary.</p></div>{message&&<div className="rounded-2xl border border-slate-200 bg-white p-4 text-sm text-slate-600">{message}</div>}<div className="grid gap-5 lg:grid-cols-[1fr_320px]"><div className="rounded-[1.6rem] border border-slate-200 bg-white p-6"><div className="flex items-center justify-between"><h2 className="text-xl font-semibold">Service progress</h2><span className="text-sm font-bold text-indigo-600">{done}/{enrollment.serviceProgress?.length||0}</span></div><div className="mt-5 space-y-3">{enrollment.serviceProgress?.length?enrollment.serviceProgress.map((item)=><div key={item.service} className="flex items-center gap-3 rounded-xl bg-slate-50 p-3"><CircleCheckBig size={18} className={item.completed?'text-emerald-600':'text-slate-300'}/><span className="text-sm font-medium text-slate-700">{item.service}</span></div>):<p className="text-sm text-slate-500">Your service plan will appear here as programme support begins.</p>}</div></div><aside className="rounded-[1.6rem] border border-slate-200 bg-white p-5"><h2 className="font-semibold text-slate-950">Programme agreement</h2><div className="mt-4 space-y-3 text-sm"><Row label="Status" value={enrollment.status}/><Row label="Agreed base fee" value={money(enrollment.successFeeAmount)}/><Row label="Payment preference" value={String(enrollment.paymentPreference).replaceAll('-',' ')}/><Row label="Terms" value={enrollment.termsVersion}/></div>{enrollment.status==='active'&&<button type="button" disabled={busy} onClick={onWithdraw} className="mt-6 w-full rounded-xl border border-slate-200 px-4 py-2.5 text-sm font-semibold text-slate-600 hover:bg-slate-50">Withdraw programme</button>}</aside></div><Trust/></section>}
function Trust(){return <div className="grid gap-3 sm:grid-cols-3"><Info icon={Target} title="Outcome-linked" text="No success fee merely for enrolling."/><Info icon={ShieldCheck} title="Candidate safeguards" text="Withdrawn offers and disputed placements are reviewed, not blindly charged."/><Info icon={UsersRound} title="Human assistance" text="Programmes add career support beyond the free marketplace."/></div>}
function Info({icon:Icon,title,text}){return <div className="rounded-2xl border border-slate-200 bg-white p-4"><Icon size={20} className="text-indigo-600"/><strong className="mt-3 block text-sm text-slate-950">{title}</strong><p className="mt-1 text-xs leading-5 text-slate-500">{text}</p></div>}
function Row({label,value}){return <div className="flex items-start justify-between gap-4 border-b border-slate-100 pb-3 last:border-0"><span className="text-slate-500">{label}</span><strong className="text-right capitalize text-slate-900">{value}</strong></div>}
