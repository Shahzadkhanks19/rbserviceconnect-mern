import { LockKeyhole, Save, ShieldCheck, UserRound } from 'lucide-react';
import { useEffect, useState } from 'react';
import { useOutletContext } from 'react-router-dom';
import CustomCheckbox from '../../components/ui/CustomCheckbox.jsx';
import { apiRequest } from '../../lib/api.js';

export default function CandidateSettingsPage(){
  const {user,setUser}=useOutletContext();
  const [account,setAccount]=useState({firstName:user.firstName||'',lastName:user.lastName||''});
  const [password,setPassword]=useState({currentPassword:'',newPassword:'',confirmPassword:''});
  const [privacy,setPrivacy]=useState(null);
  const [message,setMessage]=useState('');const [saving,setSaving]=useState('');
  useEffect(()=>{let cancelled=false;apiRequest('/candidate/privacy').then((r)=>!cancelled&&setPrivacy(r.privacy||{})).catch((e)=>!cancelled&&setMessage(e.message));return()=>{cancelled=true;};},[]);
  const saveAccount=async(e)=>{e.preventDefault();setSaving('account');setMessage('');try{const r=await apiRequest('/candidate/account',{method:'PATCH',body:JSON.stringify(account)});setUser(r.user);setMessage(r.message);}catch(err){setMessage(err.message);}finally{setSaving('');}};
  const savePassword=async(e)=>{e.preventDefault();if(password.newPassword!==password.confirmPassword){setMessage('New passwords do not match.');return;}setSaving('password');setMessage('');try{const r=await apiRequest('/candidate/password',{method:'PATCH',body:JSON.stringify({currentPassword:password.currentPassword,newPassword:password.newPassword})});setPassword({currentPassword:'',newPassword:'',confirmPassword:''});setMessage(r.message);}catch(err){setMessage(err.message);}finally{setSaving('');}};
  const savePrivacy=async()=>{setSaving('privacy');setMessage('');try{const r=await apiRequest('/candidate/privacy',{method:'PATCH',body:JSON.stringify(privacy)});setPrivacy(r.privacy);setMessage(r.message);}catch(err){setMessage(err.message);}finally{setSaving('');}};
  return <section className="space-y-6"><div className="rounded-[2rem] bg-slate-950 p-6 text-white sm:p-8"><div className="flex items-start gap-4"><span className="grid size-12 shrink-0 place-items-center rounded-2xl bg-[#879E83]"><ShieldCheck size={22}/></span><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#F3E8A2]">Account & security</p><h2 className="mt-2 text-3xl font-semibold tracking-[-.04em]">Keep your workspace identity secure.</h2><p className="mt-3 max-w-2xl text-sm leading-7 text-slate-400">Manage your account, password, recruiter visibility and communication preferences.</p></div></div></div>{message&&<div className="rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm text-slate-600">{message}</div>}<div className="grid gap-6 xl:grid-cols-2"><form onSubmit={saveAccount} className="rounded-[1.7rem] border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><span className="grid size-10 place-items-center rounded-xl bg-[#F3E8A2]/70 text-indigo-700"><UserRound size={18}/></span><h3 className="mt-4 text-xl font-semibold text-slate-950">Account details</h3><p className="mt-1 text-sm text-slate-500">Your email remains {user.email}.</p><div className="mt-6 grid gap-4 sm:grid-cols-2"><Field label="First name"><input value={account.firstName} onChange={(e)=>setAccount({...account,firstName:e.target.value})} className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-amber-400 focus:ring-4 focus:ring-amber-100"/></Field><Field label="Last name"><input value={account.lastName} onChange={(e)=>setAccount({...account,lastName:e.target.value})} className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-amber-400 focus:ring-4 focus:ring-amber-100"/></Field></div><button disabled={saving==='account'} className="mt-6 inline-flex items-center gap-2 rounded-xl bg-[#E3A341] px-5 py-3 text-sm font-semibold text-white disabled:opacity-60"><Save size={16}/>{saving==='account'?'Saving…':'Save account'}</button></form><form onSubmit={savePassword} className="rounded-[1.7rem] border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><span className="grid size-10 place-items-center rounded-xl bg-[#F3E8A2]/70 text-indigo-700"><LockKeyhole size={18}/></span><h3 className="mt-4 text-xl font-semibold text-slate-950">Change password</h3><p className="mt-1 text-sm text-slate-500">Use at least 8 characters and avoid reusing passwords.</p><div className="mt-6 space-y-4"><Field label="Current password"><input type="password" value={password.currentPassword} onChange={(e)=>setPassword({...password,currentPassword:e.target.value})} className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-amber-400 focus:ring-4 focus:ring-amber-100"/></Field><Field label="New password"><input type="password" value={password.newPassword} onChange={(e)=>setPassword({...password,newPassword:e.target.value})} className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-amber-400 focus:ring-4 focus:ring-amber-100"/></Field><Field label="Confirm new password"><input type="password" value={password.confirmPassword} onChange={(e)=>setPassword({...password,confirmPassword:e.target.value})} className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-amber-400 focus:ring-4 focus:ring-amber-100"/></Field></div><button disabled={saving==='password'} className="mt-6 inline-flex items-center gap-2 rounded-xl bg-slate-950 px-5 py-3 text-sm font-semibold text-white disabled:opacity-60"><LockKeyhole size={16}/>{saving==='password'?'Updating…':'Change password'}</button></form></div><div className="rounded-[1.7rem] border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-start gap-4"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-emerald-50 text-emerald-700"><ShieldCheck size={18}/></span><div><h3 className="text-xl font-semibold text-slate-950">Privacy & recruiter visibility</h3><p className="mt-1 text-sm leading-6 text-slate-500">Choose how verified recruiters can discover and interact with your candidate profile.</p></div></div>{privacy?<div className="mt-6 grid gap-3 md:grid-cols-2">{[['discoverableToRecruiters','Visible in recruiter candidate search','Turn this off to remove your profile from recruiter discovery.'],['showResumeToRecruiters','Show resume to recruiters','Recruiters can open your current resume from candidate search.'],['jobAlertsEnabled','Enable job-alert matching','Allow saved job alerts to generate new-role notifications.'],['recruiterMessagesEnabled','Allow recruiter messages','Control whether verified recruiters can contact you directly.']].map(([key,label,copy])=><CustomCheckbox key={key} checked={privacy[key]!==false} onChange={(checked)=>setPrivacy({...privacy,[key]:checked})} label={label} description={copy}/>)}</div>:<p className="mt-6 text-sm text-slate-500">Loading privacy preferences…</p>}<button type="button" onClick={savePrivacy} disabled={!privacy||saving==='privacy'} className="mt-6 inline-flex items-center gap-2 rounded-xl bg-[#879E83] px-5 py-3 text-sm font-semibold text-white disabled:opacity-60"><Save size={16}/>{saving==='privacy'?'Saving…':'Save privacy preferences'}</button></div></section>;
}
function Field({label,children}){return <label className="block text-sm font-semibold text-slate-700"><span className="mb-2 block">{label}</span>{children}</label>}
