import { CheckCircle2,Clock3,IndianRupee,Play,Printer,ReceiptText,RefreshCw,ShieldCheck,TriangleAlert,WalletCards,X,Zap } from 'lucide-react';
import { useCallback,useEffect,useState } from 'react';import { apiRequest } from '../../lib/api.js';
const money=(value)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(Number(value)||0);const day=(value)=>value?new Intl.DateTimeFormat('en-IN',{dateStyle:'medium'}).format(new Date(value)):'—';const label=(value)=>String(value||'').replaceAll('_',' ');let razorpayLoader;
function loadRazorpay(){if(window.Razorpay)return Promise.resolve();if(!razorpayLoader)razorpayLoader=new Promise((resolve,reject)=>{const script=document.createElement('script');script.src='https://checkout.razorpay.com/v1/checkout.js';script.async=true;script.onload=resolve;script.onerror=()=>reject(new Error('Unable to load secure Razorpay checkout.'));document.head.appendChild(script);});return razorpayLoader;}
export default function CandidateBillingPage(){const[data,setData]=useState(null),[error,setError]=useState(''),[message,setMessage]=useState(''),[receipt,setReceipt]=useState(null),[receiptBusy,setReceiptBusy]=useState(false),[paying,setPaying]=useState(''),[autoBusy,setAutoBusy]=useState('');const load=useCallback(async()=>{try{setData(await apiRequest('/candidate/billing'));setError('');}catch(err){setError(err.message||'Unable to load billing.');}},[]);useEffect(()=>{let cancelled=false;apiRequest('/candidate/billing').then(r=>{if(!cancelled)setData(r);}).catch(e=>{if(!cancelled)setError(e.message);});return()=>{cancelled=true;};},[]);const action=async(fee,type)=>{setAutoBusy(`${fee._id}-${type}`);setError('');setMessage('');try{const options=type==='enable'?{method:'POST'}:type==='disable'?{method:'DELETE'}:{method:'POST'};const suffix=type==='run'?'/demo-run':'';const r=await apiRequest(`/candidate/billing/${fee._id}/autopay${suffix}`,options);setMessage(r.message);await load();}catch(e){setError(e.message);}finally{setAutoBusy('');}};const openReceipt=async(id)=>{setReceiptBusy(true);try{const r=await apiRequest(`/candidate/billing/receipts/${id}`);setReceipt(r.receipt);}catch(e){setError(e.message);}finally{setReceiptBusy(false);}};const pay=async(fee,item)=>{setPaying(`${fee._id}-${item.number}`);setMessage('');setError('');try{await loadRazorpay();const response=await apiRequest(`/candidate/billing/${fee._id}/installments/${item.number}/order`,{method:'POST'});const c=response.checkout;if(!c?.keyId||!c?.orderId)throw new Error('Online payment checkout is not configured.');const razorpay=new window.Razorpay({key:c.keyId,amount:c.amount,currency:c.currency,name:c.name,description:c.description,order_id:c.orderId,prefill:c.prefill,theme:{color:'#E3A341'},handler:async(payment)=>{try{const verified=await apiRequest(`/candidate/billing/transactions/${response.transaction._id}/verify-payment`,{method:'POST',body:JSON.stringify(payment)});setMessage(verified.message);await load();}catch(e){setError(e.message);}finally{setPaying('');}},modal:{ondismiss:()=>setPaying('')}});razorpay.on('payment.failed',f=>{setPaying('');setError(f.error?.description||'Payment failed.');});razorpay.open();}catch(e){setError(e.message);setPaying('');}};const totals=(data?.fees||[]).reduce((s,i)=>({total:s.total+(i.billing?.totalAmount||0),paid:s.paid+(i.billing?.paidAmount||0),due:s.due+(i.billing?.outstandingAmount||0)}),{total:0,paid:0,due:0});return <section className="space-y-6"><div className="rounded-[2rem] bg-slate-950 p-6 text-white sm:p-8"><span className="inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1.5 text-xs font-bold"><WalletCards size={14}/> Career Programme billing</span><h1 className="mt-4 text-3xl font-semibold sm:text-4xl">Billing & payments</h1><p className="mt-3 max-w-2xl text-sm text-slate-300">Manage verified programme fees, secure payments and the portfolio AutoPay simulation.</p></div>{message&&<div className="rounded-2xl border border-emerald-200 bg-emerald-50 p-4 text-sm text-emerald-700">{message}</div>}{error&&<div className="rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>}{data&&<div className="grid gap-3 sm:grid-cols-3">{[[IndianRupee,'Total programme fees',totals.total],[CheckCircle2,'Paid',totals.paid],[Clock3,'Outstanding',totals.due]].map(([Icon,t,v])=><div key={t} className="rounded-2xl border bg-white p-5"><Icon size={19} className="text-slate-400"/><p className="mt-3 text-xs font-semibold text-slate-500">{t}</p><strong className="mt-1 block text-2xl">{money(v)}</strong></div>)}</div>}{!data?<div className="py-14 text-center text-sm">Loading billing…</div>:!data.fees?.length?<div className="rounded-3xl border border-dashed bg-white p-10 text-center"><ReceiptText className="mx-auto text-slate-300"/><h2 className="mt-4 font-semibold">No active billing</h2></div>:<div className="space-y-5">{data.fees.map(fee=>{const firstOpen=fee.billing?.installments?.find(i=>!['paid','waived'].includes(i.status)),auto=fee.billing?.autopay||{},active=auto.status==='active';return <article key={fee._id} className="rounded-[1.75rem] border bg-white p-5 sm:p-6"><div className="flex flex-wrap justify-between gap-4"><div><span className="rounded-full bg-indigo-50 px-3 py-1 text-xs font-bold capitalize text-indigo-700">{label(fee.paymentPreference)}</span><h2 className="mt-3 text-xl font-semibold">{fee.job?.title||'Career Programme placement'}</h2><p className="mt-1 text-sm text-slate-500">{fee.company?.name||'Employer'}</p></div><div className="text-right"><p className="text-xs font-semibold uppercase text-slate-400">Outstanding</p><strong className="text-2xl">{money(fee.billing?.outstandingAmount)}</strong></div></div>{fee.paymentPreference!=='full'&&<div className={`mt-5 rounded-2xl border p-4 ${active?'border-indigo-200 bg-indigo-50/60':'border-slate-200 bg-slate-50'}`}><div className="flex flex-wrap items-start justify-between gap-4"><div className="flex gap-3"><div className="rounded-xl bg-white p-2 shadow-sm"><Zap size={19} className={active?'text-indigo-600':'text-slate-400'}/></div><div><div className="flex flex-wrap items-center gap-2"><p className="text-sm font-bold">Portfolio AutoPay</p><span className="rounded-full bg-white px-2 py-0.5 text-[11px] font-bold capitalize">{label(auto.status||'inactive')}</span></div><p className="mt-1 max-w-xl text-xs leading-5 text-slate-600">Demo mandate architecture only. No real bank account, card or UPI mandate is created or charged.</p>{active&&<div className="mt-3 grid gap-2 text-xs text-slate-600 sm:grid-cols-3"><span><b>Next debit:</b> {day(auto.nextDebitAt)}</span><span><b>Limit:</b> {money(auto.maxDebitAmount)}</span><span><b>Last attempt:</b> {label(auto.lastAttemptStatus||'not started')}</span></div>}{auto.lastFailureReason&&<p className="mt-2 text-xs font-medium text-rose-600">{auto.lastFailureReason}</p>}</div></div><div className="flex flex-wrap gap-2">{!active&&firstOpen&&<button disabled={Boolean(autoBusy)} onClick={()=>action(fee,'enable')} className="rounded-xl bg-slate-950 px-3 py-2 text-xs font-semibold text-white"><ShieldCheck size={14} className="mr-1 inline"/>Enable demo AutoPay</button>}{active&&<><button disabled={Boolean(autoBusy)} onClick={()=>action(fee,'run')} className="rounded-xl bg-indigo-600 px-3 py-2 text-xs font-semibold text-white"><Play size={14} className="mr-1 inline"/>Run demo debit now</button><button disabled={Boolean(autoBusy)} onClick={()=>action(fee,'disable')} className="rounded-xl border bg-white px-3 py-2 text-xs font-semibold"><X size={14} className="mr-1 inline"/>Disable</button></>}</div></div></div>}<div className="mt-6 space-y-3">{fee.billing?.installments?.map(item=><div key={item.number} className="flex flex-col justify-between gap-3 rounded-2xl bg-slate-50 p-4 sm:flex-row sm:items-center"><div className="flex items-center gap-3">{item.status==='paid'?<CheckCircle2 className="text-emerald-600" size={20}/>:item.status==='overdue'?<TriangleAlert className="text-amber-600" size={20}/>:<Clock3 className="text-slate-400" size={20}/>}<div><p className="text-sm font-semibold">Instalment {item.number} · {money(item.amount)}</p><p className="text-xs text-slate-500">Due {day(item.dueAt)}{item.paidAt?` · Paid ${day(item.paidAt)}`:''}</p></div></div><div className="flex items-center gap-2"><span className="rounded-full bg-white px-3 py-1 text-xs font-bold capitalize">{label(item.status)}</span>{firstOpen?.number===item.number&&data.payment?.enabled&&<button disabled={Boolean(paying)} onClick={()=>pay(fee,item)} className="rounded-xl bg-[#E3A341] px-4 py-2.5 text-xs font-semibold text-white">{paying?'Opening…':'Pay securely'}</button>}</div></div>)}</div></article>})}</div>}{data?.transactions?.length>0&&<div className="rounded-3xl border bg-white p-5"><div className="flex items-center gap-2"><RefreshCw size={18}/><h2 className="text-lg font-semibold">Payment & AutoPay attempts</h2></div><div className="mt-4 divide-y">{data.transactions.map(item=><div key={item._id} className="flex flex-wrap justify-between gap-3 py-4"><div><p className="text-sm font-semibold">Instalment {item.installmentNumber} · {money(item.amount)}</p><p className="text-xs text-slate-500">{label(item.provider||item.method)}{item.receiptNumber?` · ${item.receiptNumber}`:''}</p>{item.notes&&<p className="mt-1 text-xs text-slate-400">{item.notes}</p>}</div><div className="flex items-center gap-3"><div className="text-right"><b className={`text-xs capitalize ${item.status==='paid'?'text-emerald-700':item.status==='failed'?'text-rose-600':'text-slate-500'}`}>{label(item.status)}</b><p className="text-xs text-slate-500">{day(item.paidAt||item.createdAt)}</p></div>{item.status==='paid'&&<button disabled={receiptBusy} onClick={()=>openReceipt(item._id)} className="rounded-xl border px-3 py-2 text-xs font-semibold">Receipt</button>}</div></div>)}</div></div>}{receipt&&<div className="fixed inset-0 z-[90] grid place-items-center bg-slate-950/60 p-4 print:static print:block print:bg-white"><div className="w-full max-w-xl rounded-3xl bg-white p-6 print:max-w-none print:rounded-none"><div className="flex justify-between print:hidden"><strong>Payment receipt</strong><button onClick={()=>setReceipt(null)}><X/></button></div><div className="mt-5 border-y py-6"><p className="text-xs font-bold uppercase tracking-[.2em] text-slate-400">Royalties Service Connect</p><h2 className="mt-2 text-2xl font-semibold">Payment Receipt</h2><p className="text-sm text-slate-500">#{receipt.receiptNumber||receipt._id}</p><div className="mt-6 grid gap-4 sm:grid-cols-2"><div><small>Amount paid</small><strong className="block">{money(receipt.amount)}</strong></div><div><small>Paid on</small><strong className="block">{day(receipt.paidAt)}</strong></div><div><small>Placement</small><strong className="block">{receipt.successFee?.job?.title||'Career Programme'}</strong></div><div><small>Provider</small><strong className="block capitalize">{label(receipt.provider||receipt.method)}</strong></div></div></div><button onClick={()=>window.print()} className="mt-5 inline-flex items-center gap-2 rounded-xl bg-slate-950 px-4 py-2 text-sm font-semibold text-white print:hidden"><Printer size={16}/>Print receipt</button></div></div>}</section>}
