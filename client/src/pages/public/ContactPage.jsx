import { CheckCircle2, HelpCircle, MessageSquareText, ShieldCheck } from 'lucide-react';
import { useState } from 'react';
import CustomSelect from '../../components/ui/CustomSelect.jsx';
import { apiRequest } from '../../lib/api.js';
import { fieldClass,firstError,isEmail,serverFieldErrors,trim,validateRequired } from '../../lib/validation.js';

const topics=[{value:'general',label:'General enquiry'},{value:'candidate',label:'Candidate support'},{value:'recruiter',label:'Recruiter support'},{value:'verification',label:'Employer verification'},{value:'accessibility',label:'Accessibility issue'}];
export default function ContactPage(){const[form,setForm]=useState({name:'',email:'',topic:'general',message:''});const[errors,setErrors]=useState({});const[status,setStatus]=useState({type:'',message:''});const[submitting,setSubmitting]=useState(false);const update=(field,value)=>{setForm(current=>({...current,[field]:value}));setErrors(current=>({...current,[field]:''}));if(status.type)setStatus({type:'',message:''});};const validate=()=>{const next={name:validateRequired(form.name,'Name',2,100),email:!trim(form.email)?'Email is required.':!isEmail(form.email)?'Enter a valid email address.':'',topic:topics.some(item=>item.value===form.topic)?'':'Choose a valid support topic.',message:validateRequired(form.message,'Message',10,3000)};setErrors(next);return !firstError(next);};const submit=async(event)=>{event.preventDefault();setStatus({type:'',message:''});if(!validate())return;try{setSubmitting(true);const response=await apiRequest('/contact',{method:'POST',body:JSON.stringify({name:trim(form.name),email:trim(form.email).toLowerCase(),topic:form.topic,message:trim(form.message)})});setStatus({type:'success',message:response?.message||'Thanks. Your message has been received.'});setForm({name:'',email:'',topic:'general',message:''});setErrors({});}catch(error){const fields=serverFieldErrors(error);setErrors(current=>({...current,...fields}));setStatus({type:'error',message:error.message});}finally{setSubmitting(false);}};return <><section className="border-b border-black/5 bg-[#F3E8A2]/55"><div className="mx-auto grid max-w-7xl gap-10 px-4 py-14 sm:px-6 lg:grid-cols-[.9fr_1.1fr] lg:items-center lg:px-8 lg:py-16"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-indigo-700">Contact RB Service Connect</p><h1 className="mt-4 text-4xl font-semibold tracking-[-0.05em] text-slate-950 sm:text-5xl">Tell us what you need help with.</h1><p className="mt-5 max-w-xl text-base leading-8 text-slate-600">Use the form for candidate support, recruiter questions, employer verification, accessibility issues, or general platform enquiries.</p></div><div className="grid gap-3 sm:grid-cols-3">{[[MessageSquareText,'One support route','Use one form and choose the right topic.'],[ShieldCheck,'Structured handling','Messages are stored for platform support review.'],[HelpCircle,'Candidate & employer help','Support both sides of the recruitment workflow.']].map(([Icon,title,copy])=><article key={title} className="rounded-2xl border border-black/5 bg-white/80 p-5"><span className="grid size-10 place-items-center rounded-xl bg-[#879E83] text-white"><Icon size={18}/></span><h2 className="mt-4 text-sm font-semibold text-slate-950">{title}</h2><p className="mt-2 text-xs leading-5 text-slate-500">{copy}</p></article>)}</div></div></section><section className="bg-slate-50 py-12 sm:py-16"><div className="mx-auto grid max-w-7xl gap-8 px-4 sm:px-6 lg:grid-cols-[.7fr_1.3fr] lg:px-8"><aside className="self-start rounded-[2rem] bg-[#879E83] p-7 sm:p-8"><p className="text-xs font-bold uppercase tracking-[0.18em] text-white/80">Before you send</p><h2 className="mt-3 text-2xl font-semibold tracking-[-0.035em] text-slate-950">Choose the most relevant topic.</h2><p className="mt-3 text-sm leading-7 text-slate-900/75">Please avoid including passwords, payment details, or unnecessary sensitive personal information.</p><div className="mt-6 space-y-3">{['Candidate account or application help','Recruiter access and employer verification','General platform or accessibility questions'].map(item=><div key={item} className="flex gap-3 rounded-2xl bg-white/20 p-4 text-sm font-medium text-slate-950"><CheckCircle2 size={18} className="mt-0.5 shrink-0"/>{item}</div>)}</div></aside><form onSubmit={submit} className="rounded-[2rem] border border-slate-200 bg-white p-6 shadow-sm sm:p-8" noValidate><p className="text-xs font-bold uppercase tracking-[0.18em] text-indigo-700">Send a message</p><h2 className="mt-2 text-2xl font-semibold tracking-[-0.03em] text-slate-950">We’ll route it to the right place.</h2>{status.message&&<div role={status.type==='error'?'alert':'status'} className={`mt-5 rounded-2xl border px-4 py-3 text-sm ${status.type==='success'?'border-emerald-200 bg-emerald-50 text-emerald-800':'border-red-200 bg-red-50 text-red-700'}`}>{status.message}</div>}<div className="mt-6 grid gap-5 sm:grid-cols-2"><Field label="Name" error={errors.name}><input value={form.name} onChange={e=>update('name',e.target.value)} onBlur={validate} maxLength="100" autoComplete="name" className={fieldClass(errors.name)} placeholder="Your name"/></Field><Field label="Email" error={errors.email}><input value={form.email} onChange={e=>update('email',e.target.value)} onBlur={validate} maxLength="254" type="email" autoComplete="email" className={fieldClass(errors.email)} placeholder="you@example.com"/></Field></div><div className="mt-5"><CustomSelect label="Topic" value={form.topic} onChange={value=>update('topic',value)} options={topics}/>{errors.topic&&<p className="mt-1.5 text-xs font-medium text-red-600">{errors.topic}</p>}</div><Field label="Message" error={errors.message} className="mt-5"><textarea value={form.message} onChange={e=>update('message',e.target.value)} onBlur={validate} maxLength="3000" rows="6" className={`${fieldClass(errors.message)} resize-none`} placeholder="How can we help?"/><span className="mt-1 block text-right text-[11px] text-slate-400">{form.message.length}/3000</span></Field><div className="mt-5 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><p className="text-xs leading-5 text-slate-400">Do not include passwords or unnecessary sensitive information.</p><button type="submit" disabled={submitting} className="rounded-xl bg-indigo-500 px-5 py-3 text-sm font-semibold text-white transition hover:bg-indigo-400 disabled:cursor-not-allowed disabled:opacity-60">{submitting?'Sending…':'Send message'}</button></div></form></div></section></>}
function Field({label,error='',className='',children}){return <label className={`block text-sm font-semibold text-slate-700 ${className}`}><span className="mb-2 block">{label}</span>{children}{error&&<span className="mt-1.5 block text-xs font-medium text-red-600">{error}</span>}</label>}
