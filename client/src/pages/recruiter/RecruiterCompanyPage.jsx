import { Building2, Save } from 'lucide-react';
import { useEffect, useState } from 'react';
import { apiRequest } from '../../lib/api.js';
const empty={name:'',website:'',industry:'',size:'',location:'',description:'',logoUrl:''};
export default function RecruiterCompanyPage(){const [form,setForm]=useState(empty);const [message,setMessage]=useState('');const [saving,setSaving]=useState(false);useEffect(()=>{let cancelled=false;apiRequest('/recruiter/company').then((r)=>{if(!cancelled&&r.company)setForm({...empty,...r.company});}).catch((e)=>!cancelled&&setMessage(e.message));return()=>{cancelled=true;};},[]);const save=async(e)=>{e.preventDefault();setSaving(true);setMessage('');try{const r=await apiRequest('/recruiter/company',{method:'PUT',body:JSON.stringify(form)});setForm({...empty,...r.company});setMessage(r.message);}catch(err){setMessage(err.message);}finally{setSaving(false);}};const set=(key)=>(e)=>setForm((f)=>({...f,[key]:e.target.value}));return <section className="space-y-6"><div className="rounded-[2rem] bg-slate-950 p-6 text-white sm:p-8"><div className="flex items-start gap-4"><span className="grid size-12 place-items-center rounded-2xl bg-[#879E83]"><Building2 size={22}/></span><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#F3E8A2]">Company profile</p><h2 className="mt-2 text-3xl font-semibold tracking-[-.04em] sm:text-4xl">Build candidate trust before they apply.</h2><p className="mt-3 max-w-2xl text-sm leading-7 text-slate-400">Your approved recruiter account owns one employer profile. Keep the company information accurate because it will appear with published jobs.</p></div></div></div>{message&&<div className="rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm text-slate-600">{message}</div>}<form onSubmit={save} className="rounded-[1.7rem] border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="grid gap-5 md:grid-cols-2"><Field label="Company name"><input value={form.name} onChange={set('name')} className="input-control" placeholder="Company name"/></Field><Field label="Website"><input value={form.website} onChange={set('website')} className="input-control" placeholder="https://..."/></Field><Field label="Industry"><input value={form.industry} onChange={set('industry')} className="input-control" placeholder="Technology, Finance, Retail..."/></Field><Field label="Company size"><input value={form.size} onChange={set('size')} className="input-control" placeholder="11–50 employees"/></Field><Field label="Location"><input value={form.location} onChange={set('location')} className="input-control" placeholder="Jaipur, Rajasthan"/></Field><Field label="Logo URL"><input value={form.logoUrl} onChange={set('logoUrl')} className="input-control" placeholder="https://..."/></Field></div><Field label="About the company" className="mt-5"><textarea value={form.description} onChange={set('description')} rows="6" className="input-control resize-none" placeholder="Tell candidates what your company does, how the team works, and what makes the opportunity meaningful."/></Field><button disabled={saving} className="mt-6 inline-flex items-center gap-2 rounded-xl bg-[#E3A341] px-5 py-3 text-sm font-semibold text-white disabled:opacity-60"><Save size={16}/>{saving?'Saving…':'Save company profile'}</button></form></section>}
function Field({label,className='',children}){return <label className={`block text-sm font-semibold text-slate-700 ${className}`}><span className="mb-2 block">{label}</span>{children}</label>}
