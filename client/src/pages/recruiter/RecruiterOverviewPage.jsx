import { BriefcaseBusiness, FileCheck2, UserCheck, UsersRound } from 'lucide-react';
import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { apiRequest } from '../../lib/api.js';
export default function RecruiterOverviewPage(){const [data,setData]=useState(null);const [error,setError]=useState('');useEffect(()=>{let cancelled=false;apiRequest('/recruiter/overview').then((r)=>!cancelled&&setData(r)).catch((e)=>!cancelled&&setError(e.message));return()=>{cancelled=true;};},[]);if(!data)return <div className="rounded-2xl border border-slate-200 bg-white p-8 text-sm text-slate-500">{error||'Loading recruiter workspace…'}</div>;const cards=[[BriefcaseBusiness,'Published jobs',data.counts.publishedJobs],[UsersRound,'Applicants',data.counts.applications],[UserCheck,'Interviews',data.counts.interviews],[FileCheck2,'Offers',data.counts.offers]];return <section className="space-y-6"><div className="rounded-[2rem] bg-[#879E83] p-6 sm:p-8"><p className="text-xs font-bold uppercase tracking-[.18em] text-white/80">Recruiter overview</p><div className="mt-3 flex flex-col gap-5 md:flex-row md:items-end md:justify-between"><div><h2 className="text-3xl font-semibold tracking-[-.04em] text-slate-950 sm:text-4xl">Your hiring pipeline at a glance.</h2><p className="mt-3 max-w-2xl text-sm leading-7 text-slate-900/70">Publish roles, review applicants, and keep hiring decisions moving from one workspace.</p></div>{data.company?<Link to="/recruiter/jobs" className="rounded-xl bg-slate-950 px-5 py-3 text-sm font-semibold text-white">Manage jobs</Link>:<Link to="/recruiter/company" className="rounded-xl bg-slate-950 px-5 py-3 text-sm font-semibold text-white">Set up company</Link>}</div></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([Icon,label,value])=><article key={label} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><span className="grid size-10 place-items-center rounded-xl bg-[#F3E8A2]/70 text-indigo-700"><Icon size={18}/></span><strong className="mt-5 block text-2xl text-slate-950">{value}</strong><span className="mt-1 block text-sm text-slate-500">{label}</span></article>)}</div><div className="rounded-[1.7rem] border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center justify-between"><div><h3 className="text-xl font-semibold text-slate-950">Recent applicants</h3><p className="mt-1 text-sm text-slate-500">Latest candidate activity across your jobs.</p></div><Link to="/recruiter/applicants" className="text-sm font-semibold text-indigo-700">View pipeline</Link></div><div className="mt-5 space-y-3">{data.recentApplications.length?data.recentApplications.map((app)=><div key={app._id} className="flex flex-col gap-3 rounded-2xl bg-slate-50 p-4 sm:flex-row sm:items-center sm:justify-between"><div><strong className="text-sm text-slate-950">{app.candidate?.firstName} {app.candidate?.lastName}</strong><p className="mt-1 text-xs text-slate-500">{app.jobSnapshot?.title} · {app.jobSnapshot?.company}</p></div><span className="self-start rounded-full bg-white px-3 py-1.5 text-xs font-semibold capitalize text-slate-600 sm:self-auto">{app.status}</span></div>):<div className="rounded-2xl border border-dashed border-slate-300 p-7 text-center text-sm text-slate-400">No candidate applications yet.</div>}</div></div></section>}
