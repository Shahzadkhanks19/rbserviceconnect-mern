import { Circle } from 'lucide-react';
export default function CustomRadioGroup({label,value,onChange,options=[],disabled=false,className=''}){return <div className={className}>{label&&<span className="mb-2 block text-sm font-semibold text-slate-700">{label}</span>}<div role="radiogroup" className="grid gap-2">{options.map((option)=>{const optionValue=option.value??option;const optionLabel=option.label??option;const active=optionValue===value;return <button key={String(optionValue)} type="button" role="radio" aria-checked={active} disabled={disabled} onClick={()=>!disabled&&onChange?.(optionValue)} className={`flex items-start gap-3 rounded-2xl border p-4 text-left transition ${active?'border-amber-400 bg-amber-50':'border-slate-200 bg-white hover:border-slate-300'} disabled:cursor-not-allowed disabled:opacity-60`}><span className={`mt-0.5 grid size-5 shrink-0 place-items-center rounded-full border ${active?'border-amber-500':'border-slate-300'}`}>{active&&<Circle size={10} className="fill-amber-500 text-amber-500"/>}</span><span><strong className="block text-sm text-slate-800">{optionLabel}</strong>{option.description&&<span className="mt-1 block text-xs leading-5 text-slate-500">{option.description}</span>}</span></button>})}</div></div>}
