import { Check } from 'lucide-react';
export default function CustomCheckbox({checked=false,onChange,label,description,disabled=false}){return <button type="button" role="checkbox" aria-checked={checked} disabled={disabled} onClick={()=>!disabled&&onChange?.(!checked)} className="flex w-full items-start gap-3 rounded-2xl border border-slate-200 bg-white p-4 text-left transition hover:border-amber-300 hover:bg-amber-50/30 disabled:cursor-not-allowed disabled:opacity-60"><span className={`mt-0.5 grid size-5 shrink-0 place-items-center rounded-md border transition ${checked?'border-amber-500 bg-amber-500 text-white':'border-slate-300 bg-white text-transparent'}`}><Check size={14}/></span><span><strong className="block text-sm text-slate-800">{label}</strong>{description&&<span className="mt-1 block text-xs leading-5 text-slate-500">{description}</span>}</span></button>}
