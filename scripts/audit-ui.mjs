import fs from 'node:fs';import path from 'node:path';
const root=path.resolve('client/src');const violations=[];const patterns=[{name:'native <select>',regex:/<select\b/g},{name:'native checkbox/radio/date/time control',regex:/type\s*=\s*["'](?:checkbox|radio|date|datetime-local|time|month|week|color)["']/g},{name:'browser alert/confirm/prompt',regex:/\b(?:window\.)?(?:alert|confirm|prompt)\s*\(/g}];
function walk(dir){for(const entry of fs.readdirSync(dir,{withFileTypes:true})){const file=path.join(dir,entry.name);if(entry.isDirectory())walk(file);else if(/\.(?:jsx|js)$/.test(entry.name)){const text=fs.readFileSync(file,'utf8');for(const pattern of patterns){for(const match of text.matchAll(pattern.regex)){const line=text.slice(0,match.index).split('\n').length;violations.push(`${path.relative(process.cwd(),file)}:${line} ${pattern.name}`);}}}}}
walk(root);if(violations.length){console.error('Custom UI audit failed:\n'+violations.map((item)=>`- ${item}`).join('\n'));process.exit(1);}console.log('Custom UI audit passed.');
